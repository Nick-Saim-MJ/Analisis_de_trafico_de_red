name: lambda26-ec-pago-prod

services:
  postgres-ec-pago:
    image: postgres:16-alpine
    container_name: lambda26-postgres-ec-pago
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${DB_NAME}
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASS}
    ports:
      - "49030:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME}"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 20s
    volumes:
      - lambda26_postgres_ec_pago_data:/var/lib/postgresql/data
    networks:
      - lambda26-ec-pago-net

  ec-pago-ms:
    build: .
    container_name: lambda26-ec-pago
    restart: unless-stopped
    depends_on:
      postgres-ec-pago:
        condition: service_healthy
    environment:
      SPRING_PROFILES_ACTIVE: ${SPRING_PROFILES_ACTIVE}
      EC_KAFKA_BOOTSTRAP_SERVERS: ${EC_KAFKA_BOOTSTRAP_SERVERS}
      DB_HOST: lambda26-postgres-ec-pago
      DB_PORT: 5432
      DB_NAME: ${DB_NAME}
      DB_USER: ${DB_USER}
      DB_PASS: ${DB_PASS}
    volumes:
      - ./logs:/app/logs
    ports:
      - "49031:8080"
    networks:
      - lambda26-ec-pago-net
      - lambda26-kafka-net

volumes:
  lambda26_postgres_ec_pago_data:

networks:
  lambda26-ec-pago-net:
    name: lambda26-ec-pago-net
  lambda26-kafka-net:
    external: true
    name: lambda26-kafka-net
