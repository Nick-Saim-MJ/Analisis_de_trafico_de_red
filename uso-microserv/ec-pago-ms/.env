# Production profile for docker-compose
SPRING_PROFILES_ACTIVE=prod

# Kafka bootstrap server used by the app inside Docker
EC_KAFKA_BOOTSTRAP_SERVERS=kafka:9092

# PostgreSQL container/app credentials
DB_NAME=db_ec_pago_ms
DB_USER=ecom
DB_PASS=ecom
